format = """$character"""

right_format = """$all"""

# Inserts a blank line between shell prompts
# add_newline = true

# Disable the package module, hiding it from the prompt completely
[package]
disabled = true

[custom.Schrodinger]
symbol = "Ψ"
format = "[$symbol$output]($style) "
style = "fg:bright-blue"
command = "echo ${SCHRODINGER: -9}"
detect_extensions = ['mae']
